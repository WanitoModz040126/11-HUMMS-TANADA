# Create a free Redis database at https://console.upstash.com
# then copy the REST URL and REST token shown on the database page.
# These two values power the persistent like counter, the per-profile/
# per-photo heart counts, and the rate limiter used on the API routes.

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
